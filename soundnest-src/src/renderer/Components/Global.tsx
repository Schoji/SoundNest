import React, { useEffect, useState } from 'react';

export const backend_address = 'http://localhost:5000';
//the purpose of this file is to make some global functions, variables or hooks that need to be active/enabled for every tsx component

